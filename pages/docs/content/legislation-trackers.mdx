import { DocsLayout } from "components";
import { getEmbedConfig } from "utils/constants";
import { BillTrackerWidget } from "components/BillTrackerWidget/BillTrackerWidget";

# Legislation Tracker

## Track multiple pieces of legislation as they progress through the legislature.

Legislation Tracker embeds allow organizations to display statuses of multiple bills in one embed, showing the date of the last action taken on the legislation.

<img
  src="https://populist-platform.s3.us-east-2.amazonaws.com/web-assets/images/legislation-tracker.jpg"
  width="600px"
/>

export default function DocsLegislationTracker({ children }) {
  return <DocsLayout currentPage="content">{children}</DocsLayout>;
}
