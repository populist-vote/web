query OrganizationBySlug($slug: String!) {
  organizationBySlug(slug: $slug) {
    id
    name
    description
    thumbnailImageUrl
    issueTags {
      id
      name
      slug
      description
    }
  }
}
