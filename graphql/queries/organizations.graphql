query OrganizationBySlug($slug: String!) {
  organizationBySlug(slug: $slug) {
    id
    name
    description
    thumbnailImageUrl
    assets {
      thumbnailImage160
      thumbnailImage400
    }
    websiteUrl
    issueTags {
      id
      name
      slug
      description
    }
  }
}
