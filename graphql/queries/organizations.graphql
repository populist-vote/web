query OrganizationBySlug($slug: String!) {
  organizationBySlug(slug: $slug) {
    id
    name
    description
    thumbnailImageUrl
    websiteUrl
    issueTags {
      id
      name
      slug
      description
    }
  }
}
