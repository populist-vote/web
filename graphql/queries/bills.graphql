query BillBySlug($slug: String!) {
  billBySlug(slug: $slug) {
    title
    description
    billNumber
    legislationStatus
    officialSummary
    populistSummary
    fullTextUrl
    issueTags {
      id
      name
      slug
    }
    sponsors {
      id
      slug
      party
      thumbnailImageUrl
      fullName
      currentOffice {
        id
        officeType
        state
        district
      }
    }
  }
}
