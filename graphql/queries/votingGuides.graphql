query votingGuidesByUserId($userId: String!) {
  votingGuidesByUserId(userId: $userId) {
    id
    title
    description
    electionId
    user {
      id
      username
      lastName
      firstName
      avatarUrl
    }
    election {
      slug
      electionDate
      title
      description
    }
  }
}