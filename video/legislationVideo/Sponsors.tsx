import { Sequence } from "remotion";
import React from "react";

// import type { Sponsor } from "generated";

export const Sponsors = () => {
  // console.log("Data in Sponsors:", billData);
  // partyavatar / candidate / office on candidate politician.office
  // helper functions in utils/politicans/computeshortofficetitle

  return (
    <Sequence>
      <div>sponsors will go here</div>
    </Sequence>
  );
};
